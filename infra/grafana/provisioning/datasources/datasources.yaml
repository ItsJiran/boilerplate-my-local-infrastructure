apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090  # Membaca env var
    access: proxy
    isDefault: true

  - name: Loki
    type: loki
    url: http://loki:3100
    access: proxy
    basicAuth: true
    basicAuthUser: ${LOKI_USER}      # Membaca env var
    secureJsonData:
      basicAuthPassword: ${LOKI_PASSWORD} # Membaca env var (Sangat disarankan!)