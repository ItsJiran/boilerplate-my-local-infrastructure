# =========================================================
# MINIO SERVER (S3) - HTTPS
# =========================================================
server {
    listen 80;
    server_name {{S3_DOMAIN}};
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name {{S3_DOMAIN}};

    ssl_certificate     {{SSL_CERT_PATH}};
    ssl_certificate_key {{SSL_KEY_PATH}};

    client_max_body_size 0;
    proxy_buffering off;
    proxy_request_buffering off;

    access_log /var/log/nginx/{{S3_DOMAIN}}_s3_access.log;
    error_log  /var/log/nginx/{{S3_DOMAIN}}_s3_error.log warn;

    location / {
        proxy_pass http://127.0.0.1:{{LOAD_BALANCER_PORT}};

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# =========================================================
# MINIO CONSOLE - HTTPS
# =========================================================
server {
    listen 80;
    server_name {{S3_CONSOLE_DOMAIN}};
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name {{S3_CONSOLE_DOMAIN}};

    ssl_certificate     {{SSL_CERT_PATH}};
    ssl_certificate_key {{SSL_KEY_PATH}};

    client_max_body_size 0;
    proxy_buffering off;
    proxy_request_buffering off;

    access_log /var/log/nginx/{{S3_CONSOLE_DOMAIN}}_s3_console_access.log;
    error_log  /var/log/nginx/{{S3_CONSOLE_DOMAIN}}_s3_console_error.log warn;

    location / {
        proxy_pass http://127.0.0.1:{{LOAD_BALANCER_PORT}};

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
